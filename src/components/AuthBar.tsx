"use client";

// Auth UI removed for v1 MVP — authentication is postponed to v2.
// The component renders nothing so existing <AuthBar /> usages compile without changes.
// See docs/Auth-Removed.md.

export function AuthBar() {
  return null;
}
